#pragma once
#include "Controller.h"
#include "Model.h"
#include "View.h"

calculationFunction(t) -> tuple<int, bool>{


	—ћќ“–» ћќƒ≈Ћ№

	return значение и сохран€ть/мен€ть
}


int main() {

	View main = View();
	main.showResults();

	getchar(); // DEBUG DEBUG DEBUG DEBUG DEBUG DEBUG DEBUG DEBUG DEBUG DEBUG

	Controller controller1 = Controller(maxAge = 5, profit = { 80, 75, 65, 60, 60, 55 }, expances = { 20, 25, 30, 35, 45, 55 }, costOfEquip = 40000);

	k = 1; // текущий год
	t = { 1, 2, 3, 4 }; // возможные t (howLongInExpluatation - сколько оборудование уже не мен€лось лет)

	for (t = 1; t < maxAge - k; t++) {
		calculationFunction(t);
	}

	storage1 = saveResults();

	k = 2;  t = { 1, 2, 3 };
	
	for (t = 1; t < maxAge - k; t++) {
		calculationFunction(t);
	}

	storage2 = saveResults();
	
	k = 3; t = { 1, 2 };

	for (t = 1; t < maxAge - k; t++) {
		calculationFunction(t);
	}

	storage3 = saveResults();

	k = 4; t = { 1,  };

	for (t = 1; t < maxAge - k; t++) {
		calculationFunction(t);
	}

	storage4 = saveResults();

	k = 5; 

	lastStep();

	maxZ = saveResults();

	//последн€€ табличка


	//-------------



	return 0;

}